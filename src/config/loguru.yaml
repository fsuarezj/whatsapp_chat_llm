handlers:
  - sink: ext://sys.stdout
    level: DEBUG
    filter: {"": "DEBUG", "chatbot": "DEBUG"}
  - sink: logs/whatsapp_chat_llm.log
    rotation: "00:00"
    delay: True
    compression: "zip"
    enqueue: true
    serialize: true
activation:
  - ["", true]
  - ["chatbot", true]
